# git-ls-files --others --exclude-from=.git/info/exclude
# Lines that start with '#' are comments.
# For a project mostly in C, the following would be a good set of
# exclude patterns (uncomment them if you want to use them):

*.[oa]
*~
*.swp
.DS_Store

# Eclipse workspace
.eclipse/

# Local archive and tmp working directories
.archive
.tmp
.settings
.manager

# Java classes and generated source files
classes/
genfiles/
target/

# CMake files
CMakeFiles/
CMakeCache.txt

# Runtime / testing
mock*
runtime/
